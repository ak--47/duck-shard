{
  "name": "duck-shard-api",
  "version": "1.1.0",
  "description": "Cloud Run API wrapper for duck-shard - DuckDB ETL tool with jq transformations",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "echo \"No tests yet\" && exit 0",
    "deploy": "./deploy.sh",
    "health": "curl -f http://localhost:${PORT:-8080}/health || exit 1"
  },
  "keywords": [
    "duck-shard",
    "duckdb",
    "etl",
    "data-processing",
    "cloud-run",
    "api",
    "jq",
    "parquet",
    "csv",
    "ndjson"
  ],
  "author": "AK",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/ak--47/duck-shard"
  }
}
